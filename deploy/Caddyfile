{$PUBLIC_DOMAIN} {
    encode zstd gzip
    handle /v1/* {
        reverse_proxy gateway:8000
    }
    handle /health* {
        reverse_proxy gateway:8000
    }
    handle /metrics {
        reverse_proxy gateway:8000
    }
    handle {
        reverse_proxy web:3000
    }
}
