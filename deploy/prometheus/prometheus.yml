global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: openknowledge-gateway
    static_configs:
      - targets:
          - gateway:8000
    metrics_path: /metrics
    scrape_interval: 30s
    scrape_timeout: 10s
